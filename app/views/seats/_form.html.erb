<%= form_with(model: seat, local: true) do |form| %>
  <% if seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% seat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :open_seat %>
    <%= form.text_field :open_seat %>
  </div>

  <div class="field">
    <%= form.label :taken_seat %>
    <%= form.text_field :taken_seat %>
  </div>

  <div class="field">
    <%= form.label :itinerary_id %>
    <%= form.text_field :itinerary_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
